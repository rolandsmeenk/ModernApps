<Page
    x:Class="ModernCSApp.MasterPage"
    IsTabStop="false"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModernCSApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="White">
        <Frame x:Name="MainFrame" CacheSize="0" />
        <Grid x:Name="NotificationDisplay" Visibility="Collapsed" VerticalAlignment="Stretch" HorizontalAlignment="Right" Margin="0,10,10,0">
        </Grid>

        <Grid x:Name="PopupDisplay" Visibility="Collapsed" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,0,0,0">
        </Grid>

        <Grid x:Name="MessageBoxDisplay" Visibility="Collapsed" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,0,0,0">
        </Grid>
    
    
    </Grid>
</Page>
